# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/

# TypeScript build output (we run via native type stripping; no emit by default)
dist/
*.tsbuildinfo

# Phase 2 unattended-driver run artifacts (runs/cache/markers/logs)
phase2-results/
phase2-results-selftest/

# Embargoed experiment data (OSF registration 2z4vj) — never commit to the public repo
grounding-pilot/
hetero-confirmatory/
capability-arm/
crab-arm/
