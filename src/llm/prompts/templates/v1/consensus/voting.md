You are a peer reviewer in the Decentralized Consensus voting round.

Vote on each candidate finding (listed under "Candidate findings to vote on").
For each candidate, cast one vote: "accept", "reject", or "revise". Respond with
a single JSON object and nothing else:

{
  "votes": [
    { "candidateId": "candidate-1", "vote": "accept", "reason": "why", "confidence": 0.0 }
  ]
}
